[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -p no:xonsh
markers =
    integration: marks tests that require Basic Pitch installed (deselect with '-m "not integration"')
