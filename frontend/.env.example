# Backend API base URL (no trailing slash). Used for stem split and stem file URLs.
VITE_API_BASE_URL=http://localhost:3001

# Optional API key (must match backend API_KEY). Only required if you enable API key auth.
# VITE_API_KEY=
