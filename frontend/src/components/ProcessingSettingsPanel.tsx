/**
 * Re-export shim — preserves existing import paths.
 * Actual implementation lives in ./processing-settings/
 */
export {
  ProcessingSettingsPanel,
  type ProcessingSettingsPanelProps,
  type LoadedStem,
} from "./processing-settings";
