# Implementation Summary

> **Note:** This file was generated by an earlier agent pass and is now superseded by the actual implementation. See `README.md` and `docs/` for current state.

## Current pipeline (as of 2026-03-18)

### 2-stem
- Kim_Vocal_2.onnx → vocals
- UVR-MDX-NET-Inst_HQ_4.onnx → instrumental (no phase inversion)
- Ultra mode only: Reverb_HQ_By_FoxJoy.onnx de-reverb post-pass on vocals

### 4-stem
1. SCNet ONNX (`USE_SCNET=1`) — tried first (~48% of Demucs CPU time)
2. Demucs ONNX (htdemucs_embedded / htdemucs_6s) — fallback
3. htdemucs subprocess — last resort

### Quality modes
| Mode | Overlap | VAD trim |
|------|---------|----------|
| Speed | 50% | Yes |
| Quality | 75% | No |
| Ultra | 75% + de-reverb | No |

### Key fixes applied
- All `stem_service/*.py` files use `from stem_service.X import` (no bare module imports)
- `hybrid.py` dead bare imports removed
- `App.tsx` `uploadState` fully destructured; `pipelineIndex` and `uploadedFile` no longer bare references
- Per-job logging: `tmp/stems/{job_id}/job.log`
- ONNX session cache: sessions loaded once, reused across requests
- `ONNXRUNTIME_NUM_THREADS=2` for t3.large (2 vCPUs)
