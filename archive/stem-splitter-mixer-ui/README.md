# Archived: stem-splitter-mixer-ui

This Vite spike simulated stem splitting in the browser and was never wired to the Burnt Beats API.

Production stem separation uses `stem_service` (Demucs ONNX) and the DJ mixer UI (`frontend/src/components/dj-mode/`).

Archived from repo root on 2026-06-03 per TEMP/stack assessment (Tier 1 repo hygiene). Do not deploy or extend this package.
