[project]
name = "burntbeats-common"
version = "0.1.0"
description = "Shared utilities for Burnt Beats Python microservices"
requires-python = ">=3.11,<3.13"
dependencies = [
    "fastapi>=0.100.0",
    "prometheus-client>=0.20.0",
]

[tool.uv]
package = true
